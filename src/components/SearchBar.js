const SearchBar = () => {

    return (
        <nav>
            <button><img src="../search.svg"/></button>
            <button><img src="../options.svg"/></button>
        </nav>
     );
}

export default SearchBar;